import { AnimatePresence, motion } from "framer-motion"; import { useGame } from "@/state/GameContext";  export default function ToastHost() {   const game = useGame();   return (     <div className="fixed top-28 left-1/2 -translate-x-1/2 z-50 flex flex-col items-center gap-2 pointer-events-none w-full px-4">       <AnimatePresence>         {game.toasts.map((t) => (           <motion.div             key={t.id}             initial={{ opacity: 0, y: -20, scale: 0.9 }}             animate={{ opacity: 1, y: 0, scale: 1 }}             exit={{ opacity: 0, scale: 0.9 }}             transition={{ duration: 0.35 }}             className="rounded-2xl border border-cyan-400/30 bg-[#070912]/90 backdrop-blur-xl px-5 py-3 flex items-center gap-3"             style={{ boxShadow: "0 0 30px rgba(80,200,255,0.25)" }}           >             <span className="text-xl">{t.icon}</span>             <div className="text-left">               <div className="font-display tracking-[0.2em] text-[11px] text-cyan-300">{t.title}</div>               <div className="text-sm text-white/85">{t.subtitle}</div>             </div>           </motion.div>         ))}       </AnimatePresence>     </div>   ); }
