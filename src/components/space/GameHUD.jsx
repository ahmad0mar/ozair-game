import { useGame } from "@/state/GameContext"; import { Coins, Globe } from "lucide-react";  export default function GameHUD() {   const game = useGame();   if (!game.started) return null;   const pct = Math.round((game.intoLevel / game.levelMax) * 100);   return (     <div className="fixed top-20 left-1/2 -translate-x-1/2 z-30 w-[94%] max-w-3xl pointer-events-none">       <div className="flex items-center gap-3 md:gap-5 rounded-2xl border border-white/12 bg-[#070912]/70 backdrop-blur-xl px-4 py-2.5 text-xs">         <span className="font-display tracking-[0.15em] text-cyan-300 whitespace-nowrap">           LVL {String(game.level).padStart(2, "0")}         </span>         <div className="flex-1 min-w-[60px]">           <div className="flex justify-between text-[10px] text-white/50 mb-1">             <span>XP</span>             <span>               {game.intoLevel}/{game.levelMax}             </span>           </div>           <div className="h-1.5 rounded-full bg-white/10 overflow-hidden">             <div               className="h-full bg-gradient-to-r from-cyan-400 to-violet-400 transition-all duration-500"               style={{ width: pct + "%" }}             />           </div>         </div>         <span className="flex items-center gap-1 text-white/80 whitespace-nowrap">           <Globe className="w-3.5 h-3.5 text-cyan-300" /> {game.discoveredCount}/8         </span>         <span className="flex items-center gap-1 text-white/80 whitespace-nowrap">           <Coins className="w-3.5 h-3.5 text-amber-300" /> {game.credits}         </span>       </div>     </div>   ); }
