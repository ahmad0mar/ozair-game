import { useGame } from "@/state/GameContext"; import { planets } from "@/data/planets"; import { Lock } from "lucide-react";  export default function SolarSystemMap() {   const game = useGame();   const go = (id) => document.querySelector("#" + id)?.scrollIntoView({ behavior: "smooth" });    return (     <section className="relative px-6 py-14">       <h3 className="text-center font-display tracking-[0.3em] text-[11px] text-white/50 uppercase mb-8">         SOLAR SYSTEM MAP       </h3>       <div className="flex items-center justify-center gap-1 md:gap-3 overflow-x-auto pb-4 max-w-5xl mx-auto">         <div className="flex flex-col items-center gap-2 shrink-0 px-1">           <div             className="w-8 h-8 rounded-full"             style={{               background: "radial-gradient(circle, #ffd86a, #ff7a18)",               boxShadow: "0 0 16px rgba(255,160,40,0.8)",             }}           />           <span className="text-[10px] text-amber-200/80">Sun</span>         </div>         {planets.map((p) => {           const discovered = game.scanned.includes(p.id);           const active = game.activePlanet === p.id;           return (             <div key={p.id} className="flex items-center gap-1 md:gap-3 shrink-0">               <span className="text-white/15 text-xs">→</span>               <button onClick={() => go(p.id)} className="flex flex-col items-center gap-2 group px-1">                 <div                   className="relative w-7 h-7 rounded-full transition-all duration-300"                   style={{                     background: discovered ? p.accent : "rgba(255,255,255,0.08)",                     boxShadow: active                       ? `0 0 0 2px #fff, 0 0 18px ${p.accent}`                       : discovered                       ? `0 0 10px ${p.accent}88`                       : "none",                     opacity: discovered ? 1 : 0.4,                   }}                 >                   {!discovered && (                     <Lock className="w-3 h-3 absolute inset-0 m-auto text-white/40" />                   )}                 </div>                 <span className={`text-[9px] tracking-wide ${discovered ? "text-white/80" : "text-white/40"}`}>                   {p.name}                 </span>               </button>             </div>           );         })}       </div>     </section>   ); }
