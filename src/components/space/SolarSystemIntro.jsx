import { motion } from "framer-motion"; import { planets } from "@/data/planets";  const orbitPlanets = planets.slice(0, 8);  export default function SolarSystemIntro() {   return (     <section       id="solar-system"       className="relative min-h-screen flex flex-col items-center justify-center px-6 py-24"     >       <motion.h2         initial={{ opacity: 0, y: 30 }}         whileInView={{ opacity: 1, y: 0 }}         viewport={{ once: true, amount: 0.5 }}         transition={{ duration: 1 }}         className="font-display text-4xl md:text-6xl font-black tracking-[0.15em] text-center"         style={{ textShadow: "0 0 40px rgba(255,180,80,0.4)" }}       >         OUR SOLAR SYSTEM       </motion.h2>        <motion.p         initial={{ opacity: 0, y: 20 }}         whileInView={{ opacity: 1, y: 0 }}         viewport={{ once: true, amount: 0.5 }}         transition={{ duration: 1, delay: 0.2 }}         className="mt-8 max-w-2xl text-center text-white/65 text-lg leading-relaxed"       >         Welcome to our solar system, a vast cosmic neighborhood centered around the Sun. From the         scorching surface of Mercury to the distant blue world of Neptune, each planet has its own         unique story, environment, and mysteries waiting to be explored.       </motion.p>        {/* Sun with orbiting planets */}       <motion.div         initial={{ opacity: 0, scale: 0.8 }}         whileInView={{ opacity: 1, scale: 1 }}         viewport={{ once: true, amount: 0.3 }}         transition={{ duration: 1.2, delay: 0.3 }}         className="relative mt-16 w-[300px] h-[300px] md:w-[440px] md:h-[440px]"       >         {/* Sun */}         <div           className="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 w-20 h-20 md:w-28 md:h-28 rounded-full"           style={{             background: "radial-gradient(circle at 50% 45%, #fff6d8 0%, #ffc24a 45%, #ff7a18 80%, #d44600 100%)",             boxShadow: "0 0 60px 20px rgba(255,160,40,0.7), 0 0 120px 40px rgba(255,120,20,0.4)",             animation: "glowPulse 4s ease-in-out infinite",           }}         />         {orbitPlanets.map((p, i) => {           const radius = 70 + i * 24;           return (             <div               key={p.id}               className="absolute left-1/2 top-1/2 rounded-full"               style={{                 width: radius * 2,                 height: radius * 2,                 transform: "translate(-50%,-50%)",                 border: "1px solid rgba(140,180,255,0.12)",                 animation: `orbit ${20 + i * 8}s linear infinite`,               }}             >               <div                 className="absolute left-1/2 top-0 -translate-x-1/2 -translate-y-1/2 rounded-full"                 style={{                   width: 10 + i * 0.6,                   height: 10 + i * 0.6,                   background: p.accent,                   boxShadow: `0 0 10px ${p.accent}`,                 }}               />             </div>           );         })}       </motion.div>     </section>   ); }
