import { useState } from "react"; import { createPortal } from "react-dom"; import { AnimatePresence, motion } from "framer-motion"; import { useGame, ACHIEVEMENTS } from "@/state/GameContext"; import { Telescope, X } from "lucide-react";  function DeepSpaceOverlay({ onExit }) {   const content = (     <div className="fixed inset-0 z-[9999] bg-[#02030a] flex items-center justify-center overflow-hidden">       <div         className="absolute inset-0 finale-zoom"         style={{           background:             "radial-gradient(40% 40% at 30% 35%, rgba(120,60,180,0.5), transparent 60%), radial-gradient(45% 45% at 70% 60%, rgba(40,90,180,0.45), transparent 60%), radial-gradient(30% 30% at 80% 20%, rgba(200,60,140,0.3), transparent 60%)",         }}       />       <div         className="absolute inset-0 opacity-70"         style={{           background:             "radial-gradient(1.5px 1.5px at 20% 30%, #fff, transparent), radial-gradient(1px 1px at 60% 70%, #fff, transparent), radial-gradient(2px 2px at 80% 20%, #cfe, transparent), radial-gradient(1px 1px at 40% 80%, #fff, transparent), radial-gradient(1.5px 1.5px at 90% 50%, #fff, transparent), radial-gradient(1px 1px at 15% 65%, #ace, transparent)",           backgroundSize: "260px 260px",         }}       />       <button         onClick={onExit}         className="absolute top-6 right-6 z-20 text-white/80 hover:text-white flex items-center gap-2 text-xs font-display tracking-[0.2em] border border-white/20 hover:border-white/40 bg-black/60 rounded-full px-4 py-2 cursor-pointer shadow-lg hover:scale-105 transition-all"       >         <X className="w-4 h-4 text-rose-400" /> RETURN       </button>       <div className="relative text-center px-6 max-w-2xl">         <Telescope className="w-12 h-12 text-violet-300 mx-auto mb-6" style={{ filter: "drop-shadow(0 0 12px #a78bfa)" }} />         <h2 className="font-display text-3xl md:text-5xl font-black tracking-[0.1em]" style={{ textShadow: "0 0 50px rgba(150,100,255,0.6)" }}>           DEEP SPACE         </h2>         <p className="mt-5 text-white/65 leading-relaxed">           Billions of stars, countless galaxies, and worlds we have yet to discover. The universe is           vast — and this is only the beginning of your journey.         </p>       </div>     </div>   );   return typeof document !== "undefined" ? createPortal(content, document.body) : content; }  export default function FinalMission() {   const game = useGame();   const [deepSpace, setDeepSpace] = useState(false);   const allDone = game.discoveredCount >= 8;   const coreAch = ACHIEVEMENTS.filter((a) => !a.secret);   const unlockedCore = coreAch.filter((a) => game.achievements.includes(a.id)).length;    return (     <section       id="universe"       className="relative min-h-screen flex flex-col items-center justify-center text-center px-6 py-24 overflow-hidden"     >       <AnimatePresence>         {deepSpace && <motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}><DeepSpaceOverlay onExit={() => setDeepSpace(false)} /></motion.div>}       </AnimatePresence>        <div         className="absolute inset-0 -z-[1] pointer-events-none finale-zoom"         style={{           background:             "radial-gradient(2px 2px at 20% 30%, #fff, transparent), radial-gradient(1px 1px at 60% 70%, #fff, transparent), radial-gradient(1.5px 1.5px at 80% 20%, #cfe, transparent), radial-gradient(1px 1px at 40% 80%, #fff, transparent), radial-gradient(2px 2px at 90% 60%, #fff, transparent), radial-gradient(1px 1px at 15% 65%, #ace, transparent)",           backgroundSize: "300px 300px",           opacity: 0.5,         }}       />        {allDone ? (         <motion.div           initial={{ opacity: 0, y: 30 }}           whileInView={{ opacity: 1, y: 0 }}           viewport={{ once: true, amount: 0.4 }}           transition={{ duration: 1 }}         >           <div className="font-display tracking-[0.3em] text-cyan-300 text-sm mb-4">             ✓ FINAL MISSION COMPLETE           </div>           <h2             className="font-display text-4xl md:text-7xl font-black tracking-[0.1em]"             style={{ textShadow: "0 0 50px rgba(120,80,255,0.5), 0 0 90px rgba(80,140,255,0.35)" }}           >             SOLAR SYSTEM EXPLORED           </h2>            <div className="mt-10 grid grid-cols-2 md:grid-cols-4 gap-4 max-w-3xl mx-auto">             {[               ["Explorer Level", game.level],               ["Planets Discovered", "8/8"],               ["Total XP", game.xp],               ["Achievements", `${unlockedCore}/${coreAch.length}`],             ].map(([label, value]) => (               <div key={label} className="rounded-2xl border border-white/10 bg-white/[0.04] backdrop-blur-xl px-4 py-5">                 <div className="font-display text-2xl md:text-3xl font-black text-white">{value}</div>                 <div className="text-[10px] tracking-[0.2em] uppercase text-white/45 mt-1">{label}</div>               </div>             ))}           </div>         </motion.div>       ) : (         <motion.div           initial={{ opacity: 0, y: 30 }}           whileInView={{ opacity: 1, y: 0 }}           viewport={{ once: true, amount: 0.4 }}           transition={{ duration: 1 }}         >           <h2             className="font-display text-4xl md:text-6xl font-black tracking-[0.12em]"             style={{ textShadow: "0 0 50px rgba(120,80,255,0.5)" }}           >             FINAL MISSION           </h2>           <p className="mt-6 max-w-xl text-white/65 mx-auto">             Complete all 8 planetary scans to finish your journey.{" "}             <span className="text-cyan-300 font-display">{game.discoveredCount}/8</span> discovered so far.           </p>         </motion.div>       )}        <p className="mt-12 max-w-2xl text-white/70 text-lg leading-relaxed">         The universe is much bigger than this.       </p>        <motion.button         initial={{ opacity: 0, scale: 0.9 }}         whileInView={{ opacity: 1, scale: 1 }}         viewport={{ once: true, amount: 0.4 }}         transition={{ duration: 0.7, delay: 0.3 }}         onClick={() => setDeepSpace(true)}         className="group mt-10 relative px-8 py-4 rounded-full font-display tracking-[0.2em] text-sm text-white flex items-center gap-3 overflow-hidden transition-transform duration-300 hover:scale-105"         style={{           background: "linear-gradient(120deg, rgba(150,80,255,0.25), rgba(80,140,255,0.25))",           border: "1px solid rgba(160,140,255,0.5)",           boxShadow: "0 0 30px rgba(150,80,255,0.4), inset 0 0 20px rgba(140,180,255,0.15)",         }}       >         <Telescope className="w-4 h-4" />         <span>CONTINUE EXPLORING</span>       </motion.button>     </section>   ); }
