# Space Explore

Fixed and reconstructed React/Vite/Tailwind project from the supplied source paste.

## Run
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```

## Main fixes
- Restored missing Vite entry/configuration files.
- Added the missing `AchievementBadge.jsx` used by Mission Control.
- Repaired the truncated `index.css`, including the missing animation definitions.
- Kept the supplied component structure and game UI.
- Added a stable `@` alias for `src`.

# ozair-game
