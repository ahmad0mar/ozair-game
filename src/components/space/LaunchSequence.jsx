import { useGame } from "@/state/GameContext"; import { Rocket } from "lucide-react";  export default function LaunchSequence() {   const game = useGame();   if (!game.launching) return null;   const lines = [10, 25, 40, 55, 70, 85];   return (     <div className="fixed inset-0 z-[60] bg-[#03040a] flex items-center justify-center overflow-hidden launch-shake">       <div className="absolute inset-0 flex items-center justify-center gap-3">         {lines.map((n) => (           <div             key={n}             className="w-px launch-stars"             style={{               height: "100%",               background: "linear-gradient(to bottom, transparent, #8fd0ff, transparent)",               opacity: 0.6,               transform: `translateX(${n - 45}px)`,             }}           />         ))}       </div>       <div className="relative text-center">         <Rocket           className="w-14 h-14 text-cyan-300 mx-auto mb-5"           style={{ filter: "drop-shadow(0 0 14px #8fd0ff)", animation: "slowFloat 1.5s ease-in-out infinite" }}         />         <div className="font-display tracking-[0.5em] text-cyan-200 text-lg">LAUNCHING</div>         <div className="text-white/50 text-sm mt-2 tracking-[0.3em]">LEAVING EARTH ORBIT...</div>       </div>     </div>   ); }
