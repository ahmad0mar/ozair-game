import { motion } from "framer-motion"; import { planets } from "@/data/planets"; import { MOONS } from "./MoonsOrbit";  export default function MoonsInfo() {   return (     <section className="relative py-28 px-6">       <div className="max-w-6xl mx-auto">         <motion.div initial={{ opacity: 0, y: 30 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: 0.3 }} className="text-center">           <div className="inline-block px-4 py-1.5 rounded-full border border-cyan-400/40 text-cyan-300 text-[11px] tracking-[0.4em] font-display mb-4">             ◐ PLANET MOONS ◯           </div>           <h2 className="font-display text-4xl md:text-6xl font-black tracking-[0.08em]" style={{ textShadow: "0 0 30px rgba(80,200,255,0.4)" }}>             MOONS OF THE SOLAR SYSTEM           </h2>           <p className="text-white/60 mt-4 max-w-xl mx-auto">Each planet hosts its own family of moons — some hiding oceans, others covered in ice or volcanoes.</p>         </motion.div>          <div className="mt-12 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">           {planets.map((p, i) => {             const m = MOONS[p.id];             const names = m?.orbit.map((o) => o.name).join(", ") || "None";             return (               <motion.div key={p.id} initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: 0.2 }} transition={{ delay: i * 0.05 }} className="rounded-2xl border border-white/10 bg-white/[0.03] p-5" style={{ boxShadow: `0 0 22px ${p.accent}22` }}>                 <div className="flex items-center gap-3">                   <div className="w-9 h-9 rounded-full shrink-0" style={{ background: p.visual.base, boxShadow: `0 0 14px ${p.accent}55` }} />                   <div>                     <div className="font-display tracking-[0.15em] text-sm" style={{ color: p.accent }}>{p.name}</div>                     <div className="text-[11px] text-white/50">{m?.total} moons</div>                   </div>                 </div>                 <div className="mt-3 text-[11px] text-white/55 leading-relaxed">                   Notable: <span className="text-white/85">{names}</span>                 </div>               </motion.div>             );           })}         </div>       </div>     </section>   ); }
