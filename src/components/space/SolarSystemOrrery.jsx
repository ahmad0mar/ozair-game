import { motion } from "framer-motion";  const ORBITS = [   { id: "mercury", r: 16, dur: 6, size: 8, color: "#b9b3a8" },   { id: "venus", r: 24, dur: 9, size: 11, color: "#ffb24d" },   { id: "earth", r: 34, dur: 12, size: 12, color: "#4ea8ff" },   { id: "mars", r: 44, dur: 16, size: 10, color: "#ff6a3d" },   { id: "jupiter", r: 60, dur: 24, size: 22, color: "#e6b572" },   { id: "saturn", r: 74, dur: 32, size: 19, color: "#ecc870", ring: true },   { id: "uranus", r: 86, dur: 42, size: 15, color: "#7fe6e8" },   { id: "neptune", r: 98, dur: 52, size: 14, color: "#4f7bff" }, ];  export default function SolarSystemOrrery() {   return (     <section className="relative py-28 px-6">       <div className="max-w-5xl mx-auto text-center">         <motion.div initial={{ opacity: 0, y: 30 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: 0.3 }}>           <div className="inline-block px-4 py-1.5 rounded-full border border-amber-400/40 text-amber-300 text-[11px] tracking-[0.4em] font-display mb-4">☉ THE WHOLE SOLAR SYSTEM ☉</div>           <h2 className="font-display text-4xl md:text-6xl font-black tracking-[0.08em]" style={{ textShadow: "0 0 30px rgba(255,200,120,0.4)" }}>LIVE ORRERY</h2>           <p className="text-white/60 mt-4 max-w-xl mx-auto">All eight planets orbiting the Sun at once. Not to scale — speeds simplified for fun.</p>         </motion.div>         <div className="relative mx-auto mt-12 w-full max-w-[680px] aspect-square">           <div className="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 w-14 h-14 rounded-full" style={{ background: "radial-gradient(circle at 50% 45%, #fff7d6, #ffd24a 40%, #ff8a1a 75%)", boxShadow: "0 0 50px 14px rgba(255,170,60,0.7)", animation: "glowPulse 3s ease-in-out infinite" }} />           {ORBITS.map((o) => {             const d = `${o.r}%`;             return (               <div key={o.id}>                 <div className="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 rounded-full border border-white/8" style={{ width: d, height: d }} />                 <div className="absolute left-1/2 top-1/2" style={{ width: d, height: d, animation: `orbit ${o.dur}s linear infinite` }}>                   <div className="absolute left-1/2 top-0 -translate-x-1/2 -translate-y-1/2 rounded-full" style={{ width: o.size, height: o.size, background: `radial-gradient(circle at 35% 35%, #fff, ${o.color})`, boxShadow: `0 0 10px ${o.color}` }} title={o.id}>                     {o.ring && <div className="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 w-[170%] h-[44%] rounded-full border border-amber-200/50" style={{ transform: "translate(-50%,-50%) rotate(-22deg)" }} />}                   </div>                 </div>               </div>             );           })}         </div>         <div className="mt-10 flex flex-wrap justify-center gap-4">           {ORBITS.map((o) => (             <div key={o.id} className="flex items-center gap-2 text-[11px] text-white/60"><span className="w-3 h-3 rounded-full" style={{ background: o.color }} /> {o.id.toUpperCase()}</div>           ))}         </div>       </div>     </section>   ); }
